#include <stdio.h>
#include <stdlib.h>
#include <math.h>

main{
int N, n;
double theta, x, y;
double pi= acos(-1.OL);
scanf ( "%d", &N);

for(n=1;n<=N; n=n+2){
for(m=0; m<=P; m++){
theta=-0.5*pi+m*(pi/p);
x=n*cos(theta);
y=n*sin(theta)-1.0;
printf("%g\t%g\n",x,y);
}
}
for(n=2;n<=N;n=n+2){
   for(m=0;m<=P;m++){
      theta=0.5*pi+m*(pi/P);
      x=n*cos(theta);
      y=n*sin(theta);
      printf("%g\t%g\n", x, y);
}
}
}

